import { ClassificacaoGeral } from './classificacao-geral.model';

describe('ClassificacaoGeral', () => {
  it('should create an instance', () => {
    expect(new ClassificacaoGeral()).toBeTruthy();
  });
});
